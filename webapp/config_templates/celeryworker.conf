[program:celeryworker]
command=celery -A paperchase.tasks worker -B --loglevel=WARNING

directory={{ app_path }}/webapp/
stdout_logfile={{ app_path }}/webapp/log/celery.log
stderr_logfile={{ app_path }}/webapp/log/celery.log
autostart=true
autorestart=true
startsecs=10